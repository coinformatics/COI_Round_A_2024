#include <bits/stdc++.h>
using namespace std;
// @author: MavrosOplarxigos

#define ll long long

int main() {
    ios::sync_with_stdio(false);
    
    ll n,q;
    cin >> n >> q;

    ll a[n];
    for(int i=0;i<n;i++){
    	cin >> a[i];
    }

    for(int i=0;i<q;i++){
        ll m;
        cin >> m;
        cout << -1 << endl;
    }

}
